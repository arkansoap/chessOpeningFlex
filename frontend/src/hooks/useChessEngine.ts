// Custom hook placeholder for chess engine interactions.
// TODO: encapsulate Stockfish evaluation calls and chess.js board state.
export function useChessEngine() {
  // Placeholder hook
  return { ready: false };
}
