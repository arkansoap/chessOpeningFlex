// Interactive chess board wrapper around react-chessboard.
// TODO: implement with react-chessboard + chess.js for move validation.
export default function ChessBoard() {
  return (
    <div className="chessboard-placeholder">
      <p>[ChessBoard] — composant échiquier interactif (à implémenter avec react-chessboard)</p>
    </div>
  );
}
