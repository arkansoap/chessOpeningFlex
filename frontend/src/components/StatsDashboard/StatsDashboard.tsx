// Charts for memorization statistics (recharts).
// TODO: render success rate / time / progress charts from /api/v1/training/stats.
export default function StatsDashboard() {
  return (
    <div className="stats-dashboard-placeholder">
      <p>[StatsDashboard] — tableaux et graphiques de statistiques</p>
    </div>
  );
}
