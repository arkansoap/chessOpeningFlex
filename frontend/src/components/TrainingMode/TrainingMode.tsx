// Training interface: shows a position and accepts a move.
// TODO: fetch question from /api/v1/training/question, validate answer.
export default function TrainingMode() {
  return (
    <div className="training-mode-placeholder">
      <p>[TrainingMode] — interface d'entraînement</p>
    </div>
  );
}
