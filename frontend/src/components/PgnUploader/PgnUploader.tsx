// Drag & drop PGN uploader.
// TODO: implement file reading + POST to /api/v1/chesscom/upload-pgn.
export default function PgnUploader() {
  return (
    <div className="pgn-uploader-placeholder">
      <p>[PgnUploader] — upload de fichiers PGN (drag &amp; drop)</p>
    </div>
  );
}
